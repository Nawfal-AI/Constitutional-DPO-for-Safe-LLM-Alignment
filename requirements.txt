torch>=2.1.0
transformers>=4.40.0
accelerate>=0.30.0
peft>=0.10.0
trl>=0.9.0
bitsandbytes>=0.43.0
datasets>=2.18.0
wandb>=0.16.0
evaluate>=0.4.1
scipy
sentencepiece
protobuf

# flash-attn is optional: it only works on Ampere+ GPUs (compute capability >= 8.0).
# Kaggle's free T4 is compute capability 7.5, so train.py auto-falls-back to
# "sdpa" attention there. Uncomment the line below only if you're running on
# an A100/H100/RTX 30xx+ and want the extra throughput.
# flash-attn --no-build-isolation
